# Clare 的私人弹药库

复刻自 `https://xiaoer-tools-wall.vercel.app/` 的静态工具墙页面，已改成 Clare 版标题与品牌文案。

## 本地预览

直接打开 `index.html`。

## 文件

- `index.html`：页面结构
- `styles.css`：视觉样式
- `app.js`：搜索、筛选、弹窗、主题切换
- `data.js`：工具数据
